# GPU-accelerated dependencies for scETM Harmony Classifier
# Install these in your conda environment for optimal performance

# Core dependencies
scanpy>=1.9.0
pandas>=1.5.0
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
anndata>=0.8.0
scipy>=1.9.0
numpy>=1.21.0
scikit-learn>=1.1.0

# Harmony integration
harmonypy>=0.0.9

# scETM
scETM

# GPU acceleration libraries
cupy-cuda11x>=11.0.0  # For CUDA 11.x (adjust version as needed)
# cupy-cuda12x>=12.0.0  # For CUDA 12.x (uncomment if using CUDA 12.x)

# RAPIDS cuML for GPU-accelerated machine learning
# Note: Install via conda for better compatibility
# conda install -c rapidsai -c conda-forge -c nvidia cuml=23.10 python=3.11 cudatoolkit=11.8

# Jupyter for notebook execution
jupyter>=1.0.0
nbconvert>=6.0.0
ipykernel>=6.0.0

# Additional utilities
tqdm>=4.64.0
matplotlib>=3.5.0
seaborn>=0.11.0
plotly>=5.0.0

# Memory optimization
psutil>=5.9.0
